<%= form_with(model: score, local: true, scope: :contest) do |form| %>
  <% if score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :point %>
    <%= form.text_field :point, id: :score_point %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :score_user_id %>
  </div>

  <div class="field">
    <%= form.label :contest_id %>
    <%= form.text_field :contest_id, id: :score_contest_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
